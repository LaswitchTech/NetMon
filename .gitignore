composer.phar
/vendor/

# Environment — never commit secrets or local settings
.env
/config/local.php

# Installation lock — managed by the installer
/storage/installed.lock

# SQLite database
/data/*.db

# macOS
.DS_Store
